@model SDGE.ApplicationCore.Entity.MembroEvento

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>MembroEvento</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="MembroEventoId" />
            <div class="form-group">
                <label asp-for="Comissao" class="control-label"></label>
                <select asp-for="Comissao" class="form-control" asp-items="(List<SelectListItem>)ViewBag.Comissao"></select>
            </div>
            <div class="form-group">
                <label asp-for="EventoId" class="control-label"></label>
                <select asp-for="EventoId" class="form-control" asp-items="ViewBag.EventoId"></select>
                <span asp-validation-for="EventoId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="MembroId" class="control-label"></label>
                <select asp-for="MembroId" class="form-control" asp-items="ViewBag.MembroId"></select>
                <span asp-validation-for="MembroId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
