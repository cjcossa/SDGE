@model IEnumerable<SDGE.ApplicationCore.Entity.Faculdade>

@{
    ViewData["Title"] = "Lista de Faculdades";
}

<div class="row">
    <div id="modalView" class="col-md-12"></div>
    <div class="col-md-12">
        @if (ViewBag.Alert != null)
        {
            <div class="alert alert-@ViewBag.Type">
                <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                    <i class="material-icons">close</i>
                </button>
                <span>
                    @ViewBag.Alert
                </span>
            </div>
        }
        <button type="button" onclick="location.href='@Url.Action("Create")'" class="btn btn-outline-success btn-sm">
            <i class="material-icons">home</i>
            Nova Faculdade
        </button>
        <div class="card">
            <div class="card-header card-header-primary">
                <h4 class="card-title">
                    @ViewData["Title"]
                </h4>

            </div>
            <div class="card-body">
                <table class="table" id="dtMaterialDesignExample">
                    <thead>
                        <tr>
                            <th>
                                Designação
                            </th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Designacao)
                                </td>
                                <td class="td-actions text-right">

                                    <button type="button" onclick="location.href='@Url.Action("Edit", new { id = item.FaculdadeId })'" rel="tooltip" title="Editar" class="btn btn-primary btn-link btn-sm">
                                        <i class="material-icons">edit</i>
                                    </button>

                                    <button type="button" data-toggle="ajax-modal" data-target="#modalConfirmDelete" data-url="@Url.Action($"Delete/{item.FaculdadeId}")" rel="tooltip" title="Remover" class="btn btn-danger btn-link btn-lg">
                                        <i class="material-icons">remove</i>
                                    </button>

                                </td>
                            </tr>
                        }
                    </tbody>

                </table>
            </div>
        </div>
    </div>
</div>